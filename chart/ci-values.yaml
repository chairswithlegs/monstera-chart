# CI-only values for helm template; do not use for real installs.
server:
  existingSecret: "ci-dummy"
instanceDomain: "ci.example.com"
uiDomain: "ci.example.com"
postgresql:
  auth:
    existingSecret: "ci-dummy"
