apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "monstera.ui.fullname" . }}-config
  labels:
    {{- include "monstera.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
data:
  config.json: |
    {
      "server_url": {{ include "monstera.server.url" . | quote }}
    }
